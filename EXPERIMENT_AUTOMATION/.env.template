# Copy to .env . Loaded by main.py via python-dotenv; never commit the result.

# SUT host (machine 3: application + instrumentation)
SUT_SSH_USER=your_ssh_username
SUT_SSH_PASSWORD=your_ssh_password

# JMeter load driver (machine 2)
JMETER_SSH_USER=your_ssh_username
JMETER_SSH_PASSWORD=your_ssh_password
